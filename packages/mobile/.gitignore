# Expo / React Native build + tooling artifacts (regenerated locally / by EAS)
.expo/
dist/
web-build/

# Native projects are generated by `expo prebuild` — keep them out of git unless
# you intentionally adopt the bare workflow.
/ios/
/android/

# Metro / Expo caches
*.log
.expo-shared/

# macOS
.DS_Store

# Local env overrides for the API base, etc.
.env*.local
