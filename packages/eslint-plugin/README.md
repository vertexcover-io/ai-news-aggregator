# @newsletter/eslint-plugin

Custom ESLint rules enforcing project conventions for the AI Newsletter Aggregator monorepo. Rules cover package boundaries, the repository pattern, dotenv bootstrapping, and other recurring traps documented under `.claude/rules/learnings/`. See `docs/rules/README.md` for the decision tree on where new rules should live.
