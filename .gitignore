node_modules/
dist/
.playwright-mcp/
.env
.env.test
.env.harness
.env.*
!.env.example
!.env.test.example
deployment/.env.prod
runtime.env
*.tsbuildinfo
.turbo/
docs/*
!docs/plans/
!docs/mocks/
!docs/mocks/**

# Unified .harness layout: knowledge/ + features/ are committed; runtime/ is per-run scratch
.harness/runtime/
# per-run context-map sync report — regenerated every sync, never shipped
.harness/knowledge/context/.sync-report.md

.worktrees/

# Playwright per-run output (traces, screenshots) — regenerates every run
packages/web/test-results/

# ranking-eval-pipeline: LLM response cache is per-developer, never committed
evals/ranking/cache/

# Reader-feedback campaign recipient exports (PII — never commit)
/confirmed_subscribers.txt
/subscribers_with_names.csv

# AGENTLOOP tenant-0 migration: pre-migration row-count snapshot (operator artifact)
agentloop-migration-counts.json

# Loose demo/QA screenshots dropped in the repo root — scratch, never committed
# (root-anchored so .harness/**/verification/screenshots/*.png stay tracked)
/*.png
.harness/.linear-config.json
